# CommandCabin v1.0.9

修复“跟随系统”主题不同步，导致 Windows 已切换为浅色时，启动器仍显示深色的问题。

- 启动器和设置窗口使用同一次读取的 Windows 应用主题，保持颜色一致。
- 窗口隐藏后重新打开时刷新主题；窗口打开期间也会跟随系统切换，通常在一秒内更新。
- 标题栏与窗口内容同步切换。手动选择“浅色”或“深色”时，继续保持指定的主题。

可在设置页检查更新，下载完成后点击“立即安装”，也可直接下载安装包。
安装包附带同名 `.sha512` 校验文件，可用于核对下载完整性。
安装包未签名，Windows 可能提示确认运行。
